{
  "schema_version": 1,
  "task_id": "lock-masked-dto",
  "description": "Locked DTOs mask content and asset paths",
  "prompt": "Fix src/lock_dto.rs. `to_dto` must preserve content and audio_path only for an unlocked note. A locked note must return None for both sensitive fields so the frontend cannot hand an asset path to convertFileSrc.",
  "source": {
    "kind": "fixture",
    "initial": "fixtures/lock-masked-dto/initial"
  },
  "allowed_paths": ["src/lock_dto.rs"],
  "scaffold_files": [".claude/rules/lock-model.md"],
  "expected_change": true,
  "graders": [
    {"id": "locked-field-mask", "script": "graders/smoke.py", "timeout_seconds": 30}
  ],
  "fake": {
    "good_overlay": "fixtures/lock-masked-dto/good",
    "bad_overlay": "fixtures/lock-masked-dto/bad",
    "good_response": "Both content and audio_path are now gated by the same unlocked decision.",
    "bad_response": "Masked note content while preserving the audio field."
  }
}
