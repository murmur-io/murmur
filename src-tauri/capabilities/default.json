{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. No fs/shell exposed to the webview.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
