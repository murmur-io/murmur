{
  "schema_version": 1,
  "task_id": "secret-sk-proj",
  "description": "Secret scanning recognizes project-scoped and legacy OpenAI token shapes",
  "prompt": "Fix tools/secret_scan.py. The current regex misses long project-scoped OpenAI tokens because their project prefix contains an extra hyphen. Detect both project-scoped and legacy long token forms while ignoring short documentation placeholders. Keep the CLI contract: exit non-zero when a secret is found.",
  "source": {
    "kind": "fixture",
    "initial": "fixtures/secret-sk-proj/initial"
  },
  "allowed_paths": ["tools/secret_scan.py"],
  "expected_change": true,
  "graders": [
    {"id": "project-token-pattern", "script": "graders/smoke.py", "timeout_seconds": 10}
  ],
  "fake": {
    "good_overlay": "fixtures/secret-sk-proj/good",
    "bad_overlay": "fixtures/secret-sk-proj/bad",
    "good_response": "Scanner covers project-prefixed and legacy long tokens without matching short placeholders.",
    "bad_response": "Added a project-only token regex."
  }
}
