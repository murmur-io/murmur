{
  "schema_version": 1,
  "task_id": "stale-receipt-hash",
  "description": "A PASS receipt is bound to the exact current tree hash",
  "prompt": "Fix harness/receipt.py so a PASS receipt records the tree hash supplied at creation and verification rejects the receipt after the current tree hash changes. Do not accept verdict text alone.",
  "source": {
    "kind": "fixture",
    "initial": "fixtures/stale-receipt-hash/initial"
  },
  "allowed_paths": ["harness/receipt.py"],
  "expected_change": true,
  "graders": [
    {"id": "freshness-binding", "script": "graders/smoke.py", "timeout_seconds": 10}
  ],
  "fake": {
    "good_overlay": "fixtures/stale-receipt-hash/good",
    "bad_overlay": "fixtures/stale-receipt-hash/bad",
    "good_response": "Bound PASS receipts to tree_sha and made verification fail closed on mismatches.",
    "bad_response": "The receipt still checks PASS."
  }
}
