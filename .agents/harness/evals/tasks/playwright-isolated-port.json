{
  "schema_version": 1,
  "task_id": "playwright-isolated-port",
  "description": "Playwright uses a harness-owned port and never reuses a stale server",
  "prompt": "Fix playwright.config.ts for parallel isolated worktrees. Require MURMUR_E2E_PORT from the harness, interpolate it into baseURL and webServer.url, fail clearly if missing, and never reuse an existing server.",
  "source": {
    "kind": "fixture",
    "initial": "fixtures/playwright-isolated-port/initial"
  },
  "allowed_paths": ["playwright.config.ts"],
  "expected_change": true,
  "graders": [
    {"id": "isolated-webserver", "script": "graders/smoke.py", "timeout_seconds": 10}
  ],
  "fake": {
    "good_overlay": "fixtures/playwright-isolated-port/good",
    "bad_overlay": "fixtures/playwright-isolated-port/bad",
    "good_response": "The config now requires the harness port and starts a fresh server for each trial.",
    "bad_response": "Disabled server reuse but kept the fixed port."
  }
}
